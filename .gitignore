# ---- HBPC Automation Project .gitignore ----
# OS / Editor cruft
Thumbs.db
Desktop.ini
*.lnk

# VS Code workspace settings (keep root workspace file, ignore local settings folder)
.vscode/


# Node / JS (if later added)
node_modules/
*.log
npm-debug.log*
yarn-error.log*

# Python (future use)
__pycache__/
*.py[cod]
*.egg-info/

# PowerShell transient files
*.ps1xml

# Archives / exports
# Ignore entire archives directory recursively (historical baseline + forensic diffs not versioned going forward)
archives/

# Explicit .git ignore (redundant: Git never tracks .git/, added for clarity per governance request)
.git/

# Removed *.zip ignore to allow flow export packages in version control
*.tar
*.gz

# Secrets / environment (if introduced later)
.env
.env.*

# Temporary diff / scratch
scratch/
.tmp/

# Keep flow export bundles (forensic & rollback) now tracked
# flow_export_*/

# End of file
\n+# ----- Sensitive Artifacts (must never be reintroduced) -----
# Folder contains hash inventory and related metadata not for public sharing
docs/metadata/
# Script enumerates and verifies hashes; keep internal only
scripts/verify_hashes.ps1