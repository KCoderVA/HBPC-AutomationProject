# ---- HBPC Automation Project .gitignore ----
# OS / Editor cruft
Thumbs.db
Desktop.ini
*.lnk

# VS Code workspace settings (keep root workspace file, ignore local settings folder)
.vscode/
.archives/*

# Node / JS (if later added)
node_modules/
*.log
npm-debug.log*
yarn-error.log*

# Python (future use)
__pycache__/
*.py[cod]
*.egg-info/

# PowerShell transient files
*.ps1xml

# Archives / exports (retain .zip solution exports for tracking)
# Removed *.zip ignore to allow flow export packages in version control
*.tar
*.gz

# Secrets / environment (if introduced later)
.env
.env.*

# Temporary diff / scratch
scratch/
.tmp/

# Keep flow export bundles (forensic & rollback) now tracked
# flow_export_*/

# End of file